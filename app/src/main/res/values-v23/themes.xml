<resources>

    <style name="Theme.Walletproject" parent="Base.Theme.Walletproject">
        <!-- Transparent system bars for edge-to-edge -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">#00BCD4</item> <!-- Atualizado para a cor azul -->
        <item name="android:windowLightStatusBar">false
        </item> <!-- Para texto branco na status bar -->
    </style>
</resources>
